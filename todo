css: use sans-serif font!!! (Check in Google Chrome)

links-admin: bool published. text comment (internal comment)
links-category: add internal comment
                Publikasjoner -> kultur?
		  nei, flytt High-Times o.l. til Kultur

add published to (almost) every model. need index since always will
use published=True (or can use custom manager).
or can use model meta: index_together ?

normal.no/nyhetsbrev/4		# issue #4

http://normal.no/opprop/
  unique(name,city)	# unique_together = ("driver", "restaurant")
  titlecase all fields
  Date -> DateTime
  cache controll: no-cache, etags, other?
  public view:
    show date
    reverse sort order

http://normal.no/bli-medlem/
  zipcode => integer field? validate 0-9999?

remove static/ dir before collectstatic?
  else looks like old files are kept

collectstatic --ignore=\*.less	# only need all.css (for now)

no-cache and/or Expires: -1 on generated pages

tests: lessc --lint *.less

redirect after form posting, so not posting twice / browser shows warning

redirect www.normal.no -> normal.no ?

strip all fields (e.g., news_article.body)

git: only allow fast-forward updates?

add requirements.txt
  pip freeze > requirements.txt

mv django/tools tools ?

http://www.websiteoptimization.com/speed/tweak/redirect/  ??

turn of .htaccess support in apache

enable broken link notification middleware.
RewriteRule pattern -  [R=404] [other_flags]	# favicon.ico robots.txt ++

Python has class calendar.HTMLCalendar([firstweekday])

parskip = 50% of line height

not needed on production server:
  static/js/less-1.4.1.min.js
  static/js/less.js

# The [G] flag forces the server to return a 410 Gone status with the
# response. This indicates that a resource used to be available, but is
# no longer available.
RewriteRule oldproduct - [G,NC]
Do this for old news archive?
  /cgi-bin/searchnews/
  /cgi-bin/rss
  /cgi-bin/viewnews?view=1242825757

/gmm/2005	redirect to old site?

http://stackoverflow.com/questions/7965893/django-internationalization
LOCALE_PATHS
